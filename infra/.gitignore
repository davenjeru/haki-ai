# Local state files (used with -backend=false)
*.tfstate
*.tfstate.backup
local.tfstate

# Local variable overrides — contains environment = "local"
terraform.tfvars.local

# Generated by make local-init — overrides S3 backend with local file
local_backend_override.tf

# Terraform internals
.terraform/
.terraform.lock.hcl
