# Production environment for `vite build`.
# Committed so `npm run build` produces a build wired to prod out of the box.
# Rotate the URL here if the API Gateway is ever re-created.

VITE_API_BASE_URL=https://b78k6llyt2.execute-api.us-east-1.amazonaws.com
